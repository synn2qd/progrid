ProGrid — Luxury Hyper-Bold build

Start at index.html (password gate).
Password: lockedin

Notes:
- This is a static site (HTML/CSS/JS).
- Page transitions are clean (fade-in on load). No overlay.
- Password requires re-entry on refresh/reopen (sessionStorage clears on reload).


V2 updates:
- Embedded thumbnails directly (no JS) so they always show.
- Added signature typography moments + selective section + authority cues.
